# Database Configuration
database:
  host: "localhost"
  port: 3306
  user: "root"
  password: "password"
  database: "instock"

postgresql:
  host: "localhost"
  port: 5432
  user: "instock"
  password: "instock_pass"
  database: "instock"

# Tushare API Configuration
tushare:
  token: "a417d287cdb8a1693af8d9d2aef6c1c915f060a72f3a7ffbf1b2b59eb8e0"

# Redis Configuration (optional, for caching)
redis:
  host: "localhost"
  port: 6379
  db: 0

# Logging Configuration
logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
