# RB2-01

> 更新时间: 2026-04-07 00:20 CST
> 执行者: Agent A
> 状态: review

## 任务目标

完成后端运行时收口，移除超出 `M0` 的 router 与 scheduler/job 入口。

## 已完成

- 已有 `RB1-04` 作为上游输入，明确本任务优先收口:
  - `app/api/routers/__init__.py`
  - `app/jobs/scheduler.py`
  - `app/main.py`（如存在超范围挂载）
- 已从运行时入口移除超出 `M0` 的 router / job 挂载。

## 关键结论

### 已修改文件

- `app/api/routers/__init__.py`
- `app/main.py`
- `app/jobs/scheduler.py`

### 具体移除项

#### `app/api/routers/__init__.py`

已移除以下 router 的导入与 `include_router(...)` 挂载:

- `events_router`
- `alert_router`
- `strategy_social_router`
- `optimization_router`
- `report_router`

#### `app/main.py`

已移除:

- `events_router` 的导入
- `app.include_router(events_router.router, prefix="/api/v1")`

说明:
- `app/main.py` 当前只直接挂载了 `events_router` 这一项超范围入口，没有 `alert / strategy_social / optimization / report` 的直接挂载。

#### `app/jobs/scheduler.py`

已移除调度任务:

- `check_alerts`
  - `func="app.jobs.tasks.alert_checker:check_all_alerts"`
  - `trigger="interval", minutes=5`

### 当前效果

- 后端运行时入口已不再暴露 `events / alert / strategy_social / optimization / report` 相关 router。
- 调度器已不再主动启动预警检查任务。

## 剩余动作

1. 由 `RB2-04` 检查是否还有残余 import / wiring 继续引用这些延后模块。
2. 由 `RB3-01` 承接代码级清理，删除已不再挂载的后端模块。
3. 后续回归时验证 `/api/v1/events` 与预警相关运行时行为已不可达。

## 残余风险

- 本任务只做“运行时入口收口”，没有删除后端实现文件；相关模块仍存在于仓库中，需由 `RB3-01` 清理。
- `app/main.py` 仍采用直接 `include_router(...)` 方式而非统一复用 `app.api.routers.router`，后续若新增入口，存在再次分叉挂载的风险。
- 未在本任务中执行测试；需要在后续 `RB4-01` 中验证 API 与调度行为无回归。

## 证据

- `docs/rebaseline_artifacts/RB1-04.md`
