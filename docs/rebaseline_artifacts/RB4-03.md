# RB4-03

> 更新时间: 2026-04-07 12:55 CST
> 执行者: Codex
> 状态: review

## 任务目标

对 `M0` 核心入口做浏览器 smoke，并留下联调证据。

## 环境

- 前端当前源码: `http://127.0.0.1:3304`
- Docker dev 前端: `http://localhost:3002`
- 浏览器会话: `agent-browser --session-name m0rb4`
- 登录用户: `rb4smoke`

## 已验证

- 登录链路:
  - 首次访问 `/` 正确重定向到登录页。
  - 通过 UI 注册并登录 `rb4smoke` 成功。
- Dashboard:
  - `http://127.0.0.1:3304/`
  - 标题: `首页工作台 - 智能股票分析平台`
  - 快照中保留 `查看股票列表`、`管理关注`、`进入筛选页`、`查看回测页`。
  - 快照中未出现通知铃、报告、参数优化、策略市场入口。
- Selection:
  - `http://127.0.0.1:3304/selection`
  - 标题: `策略选股 - 智能股票分析平台`
  - 快照中保留 `保存条件`、`保存为策略`、`开始筛选`、快捷模板与筛选条件。
- Backtest:
  - `http://127.0.0.1:3304/backtest`
  - 标题: `策略回测 - 智能股票分析平台`
  - 快照中保留 `加载模板`、`保存策略`、`复制链接`、`运行回测`、策略模板下拉。
  - 快照中未出现公共策略库 / 参数优化标签页。
- Attention:
  - `http://127.0.0.1:3304/attention`
  - 标题: `我的关注 - 智能股票分析平台`
  - 快照中保留搜索框与 `添加关注`。
  - 快照中未出现快速预警入口。
- Stocks / StockDetail:
  - `http://127.0.0.1:3304/stocks`
  - 标题: `股票列表 - 智能股票分析平台`
  - 从股票列表点击首个 `查看图表` 后进入 `http://127.0.0.1:3304/stock/300006`
  - 标题: `个股详情 - 智能股票分析平台`
  - 快照中保留 K 线周期、复权、指标、`回测策略`、`添加到关注`、`分析形态`。

## 注意事项

- `http://localhost:3002` 起初是旧 Docker 前端镜像，快照中仍显示旧的通知铃、报告、优化、策略市场入口。
- 已执行 `docker compose -f docker-compose.dev.yml up -d --build frontend` 重建 Docker dev 镜像。
- 重建后 `instock_dev_app` 与 `instock_dev_frontend` 均为 `healthy`。
- 重建后 `http://localhost:3002` 的 Dashboard 快照只保留 `查看股票列表`、`管理关注`、`进入筛选页`、`查看回测页`，未再出现旧策略市场、报告、优化入口。
- 重建后已补充打开:
  - `http://localhost:3002/selection`
  - `http://localhost:3002/backtest`
  - `http://localhost:3002/attention`
- 浏览器旧会话曾短暂显示旧入口；直接检查镜像内 `/usr/share/nginx/html/assets` 与 `Dashboard-*.js` 未发现旧字符串，重新打开新会话后页面快照正常，判断为浏览器会话缓存。

## 证据命令

- `agent-browser --session-name m0rb4 open http://127.0.0.1:3304/`
- `agent-browser --session-name m0rb4 snapshot -i`
- `agent-browser --session-name m0rb4 open http://127.0.0.1:3304/selection`
- `agent-browser --session-name m0rb4 open http://127.0.0.1:3304/backtest`
- `agent-browser --session-name m0rb4 open http://127.0.0.1:3304/attention`
- `agent-browser --session-name m0rb4 open http://127.0.0.1:3304/stocks`
- `docker compose -f docker-compose.dev.yml up -d --build frontend`
- `docker compose -f docker-compose.dev.yml ps frontend app`
- `curl -i http://localhost:3002`
- `curl -i http://localhost:8001/health`
- `agent-browser --session-name m0docker3 open http://localhost:3002/`
- `agent-browser --session-name m0docker3 open http://localhost:3002/selection`
- `agent-browser --session-name m0docker3 open http://localhost:3002/backtest`
- `agent-browser --session-name m0docker3 open http://localhost:3002/attention`
