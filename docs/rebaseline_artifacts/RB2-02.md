# RB2-02

> 更新时间: 2026-04-07 00:00 CST
> 执行者: Codex
> 状态: review

## 任务目标

完成前端路由与导航入口收口，移除超出 `M0` 的独立页面入口与菜单入口。

## 已完成

- 已有 `RB1-04` 作为上游输入，明确本任务优先收口:
  - `web/src/router/index.ts`
  - `web/src/components/layout/AppHeader.vue`
  - `web/src/components/layout/AppSidebar.vue`
- 已移除前端独立路由入口:
  - `Alerts`
  - `StrategyMarket`
  - `Optimization`
  - `Reports`
  - `ReportDetail`
  - `ReportPreferences`
- 已移除 `AppHeader` 中的通知铃铛入口与对应导入。
- 已复核 `AppSidebar.vue` 当前内容，确认其中不存在新增的 `P3` 导航入口，因此本轮未修改。

## 关键结论

### 已改文件

- `web/src/router/index.ts`
- `web/src/components/layout/AppHeader.vue`
- `docs/rebaseline_artifacts/RB2-02.md`

### 具体移除项

- 路由级入口移除:
  - `/alerts`
  - `/strategies`
  - `/optimization`
  - `/reports`
  - `/reports/:id`
  - `/report-preferences`
- 导航级入口移除:
  - `AppHeader` 中 `NotificationBell` 组件挂载
  - `NotificationBell` 对应导入

### 本轮未动但已确认

- `web/src/components/layout/AppSidebar.vue`
  - 当前只承载关注列表和快速操作，没有本轮目标中的超范围独立导航入口
  - 可在后续更大范围清理时再复查，但 `RB2-02` 不需要改它

### 残余风险

- 页面内部仍存在超范围跳转和数据依赖，尚未在本任务处理:
  - `Dashboard.vue` 仍可能保留 `/reports`、`/optimization` 等跳转
  - `Backtest.vue` 仍保留 `public` / `optimization` 标签页分支
  - `Attention.vue` 仍保留快速预警交互
- 因此本任务只完成“独立路由与导航入口收口”，片段级收口需交给 `RB2-03`。

## 剩余动作

1. 将 `Dashboard.vue`、`Backtest.vue`、`Attention.vue` 的片段级入口清单交给 `RB2-03`。
2. 在 `RB2-04` 清理残余路由名、API 调用和死引用。
3. 在 `RB3-02` 再删除对应的页面与组件文件本体。

## 证据

- `docs/rebaseline_artifacts/RB1-04.md`
- `web/src/router/index.ts`
- `web/src/components/layout/AppHeader.vue`
- `web/src/components/layout/AppSidebar.vue`
