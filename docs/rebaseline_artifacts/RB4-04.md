# RB4-04

> 更新时间: 2026-04-07 13:10 CST
> 执行者: Codex
> 状态: review

## 任务目标

整理 `M0 rebaseline` 治理包的合并说明，明确本轮变更范围、验证证据、已知注意事项和后续合并前检查点。

## 变更范围

- 文档治理:
  - 新增 `docs/REBASELINE_M0_PLAN.md`、`docs/REBASELINE_M0_TRACKER.md` 和 `docs/rebaseline_artifacts/*`。
  - 将阶段文档迁移到 `docs/milestones/`，将 v1 历史文档迁移到 `docs/archive/`。
  - 更新 `docs/README.md`、`docs/EXECUTION_PLAN.md`、`docs/PRD.md`、`docs/ROADMAP.md` 的导航和当前基线说明。
- 后端运行时与代码清理:
  - 从路由、调度入口移除 `events`、`alert`、`strategy_social`、`optimization`、`report` 相关入口。
  - 删除对应 router/service/model/schema/job 模块。
  - 清理 `app/models/__init__.py` 等残余导入。
- 前端运行时与代码清理:
  - 移除 `/alerts`、`/strategies`、`/optimization`、`/reports`、`/reports/:id`、`/report-preferences` 等超范围路由。
  - 删除 `Alerts`、`Reports`、`Optimization`、`StrategyMarket` 等页面，以及 `NotificationBell`、`StrategyCard` 等组件。
  - 从 `Dashboard`、`Backtest`、`Attention`、`Selection`、`StockDetail` 移除超范围入口与埋点残余。
- 脚本与测试清理:
  - 将 `Makefile:init-db` 改为当前 `M0` 的 `app.database.init_db()` 初始化入口。
  - 删除 M1/Timescale/回填过渡脚本和根目录 quickfix 文档。
  - 删除已失效的 `tests/test_events_api.py`。

## 验证证据

- `make init-db`
  - 结果: 通过。
- `.venv/bin/python -c "import app.main; print('app.main import ok')"`
  - 结果: 通过。
- `.venv/bin/python -m pytest tests/test_router_endpoints.py -q`
  - 结果: `10 passed, 1 warning`。
- `.venv/bin/python -m pytest tests -q`
  - 结果: `129 passed, 1 warning`。
- `cd web && npm run typecheck`
  - 结果: 通过。
- `cd web && npm run build`
  - 结果: 通过。
  - 已知 warning: Sass legacy JS API、Sass `@import` deprecation、`charts-vendor` chunk size。
- 浏览器 smoke:
  - 环境: `http://127.0.0.1:3304`
  - 结果: Dashboard、Selection、Backtest、Attention、Stocks、StockDetail 主链路通过。
- Docker dev smoke:
  - 命令: `docker compose -f docker-compose.dev.yml up -d --build frontend`
  - 结果: `instock_dev_app` 与 `instock_dev_frontend` 均为 `healthy`。
  - 环境: `http://localhost:3002`
  - 结果: Dashboard、Selection、Backtest、Attention 主链路通过。

## 已知注意事项

- `http://localhost:3002` 起初是旧 Docker 前端镜像，已通过 `docker compose -f docker-compose.dev.yml up -d --build frontend` 重建并补充 smoke。若后续换环境或换 tag 部署，仍需对对应镜像再做一次 smoke。
- `docs/milestones/` 和 `docs/rebaseline_artifacts/` 会保留历史阶段、历史脚本和已删除功能的文字记录；这些引用是审计上下文，不代表 active runtime 仍然保留对应实现。
- clean rebaseline PR 只基于 `origin/main` 现有资产；旧长分支上的额外 `M1_TASK_WS*.md` 任务包和 M1 启动/跟踪脚本不纳入本次 PR。
- 浏览器 smoke 中创建了本地测试用户 `rb4smoke`。

## Stage-aware review

- clean cherry-pick 暂存区当前显示:
  - `115 files changed`
  - `2009 insertions`
  - `9401 deletions`
- `docs/` 根目录 deleted entries 已逐一对照到 `docs/archive/` 或 `docs/milestones/` 下的同名文件。
- 多数迁移文件内容 hash 与 `HEAD` 一致；少数 `CHANGED` 项为路径修正，例如:
  - `docs/M1_*.md` -> `docs/milestones/m1/M1_*.md`
  - `docs/DATA_LAYER_REPORT.md` -> `docs/milestones/m1/DATA_LAYER_REPORT.md`
  - `QUICKFIX_M1_TUSHARE_TOKEN.md` -> `docs/milestones/m1/M1_TUSHARE_TOKEN_BLOCK.md`
- Active 顶层治理文档中未再发现旧根路径链接:
  - `docs/README.md`
  - `docs/EXECUTION_PLAN.md`
  - `docs/PRD.md`
  - `docs/ROADMAP.md`
  - `docs/REBASELINE_M0_PLAN.md`
  - `docs/REBASELINE_M0_TRACKER.md`

## 合并前建议

1. 重新确认 `docs/archive/`、`docs/milestones/`、`docs/rebaseline_artifacts/` 全部纳入提交。
2. 提交说明建议按三类组织: runtime 收口、物理删除、验收治理文档。
