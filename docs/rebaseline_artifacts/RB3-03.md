# RB3-03

> 更新时间: 2026-04-07 12:05 CST
> 执行者: Codex
> 状态: review

## 任务目标

删除或归档超范围文档与脚本，重点处理 `M1` 启动资产、Timescale 过渡脚本、批量回填脚本和 M1 跟踪脚本。

## 已完成

- 已确认 `M1` / `Phase3` / `Phase4` 文档已迁移到 `docs/milestones/`，v1 归档已迁移到 `docs/archive/`。
- 已复扫待清理脚本名在仓库内的引用。
- 已按方案 A 将 `Makefile:init-db` 改为当前 `M0` 初始化入口:
  - `python -c "import asyncio; from app.database import init_db; asyncio.run(init_db())"`
- 已删除 `M1` / Timescale / 回填过渡脚本:
  - `scripts/check_m1_readiness.py`
  - `scripts/convert_to_hypertable.py`
  - `scripts/full_backfill_tushare.py`
  - `scripts/init_timescaledb.py`
  - `scripts/prepare_daily_bars_for_backfill.py`
  - `scripts/restore_daily_bars_compression.py`
  - `scripts/run_tushare_backfill.py`
  - `scripts/test_backfill.py`
- 已删除根目录 `QUICKFIX_M1_TUSHARE_TOKEN.md`，并将里程碑文档中的引用改到 `docs/milestones/m1/M1_TUSHARE_TOKEN_BLOCK.md`。
- 旧长分支上的额外 M1 启动/跟踪脚本不属于 `origin/main` clean rebaseline PR 范围，因此未列入本次删除证据。

## 当前结论

- 文档侧已经完成“从 active docs 迁出”的主要工作。
- 脚本侧已完成“从 active scripts 退场”。
- `make init-db` 现在代表当前 `M0` 的普通数据库初始化，不再承接 Timescale / Alembic / M1 语义。

## 剩余动作

1. 在 `RB3-04` 做一次全仓测试/导入复扫，区分里程碑归档文档中的历史引用与 active 入口引用。
2. 若未来重启 M1，应从 `docs/milestones/m1/` 重新恢复或重建启动脚本，而不是从 M0 active scripts 直接调用。

## 证据

- `Makefile`
- `docs/EXECUTION_PLAN.md`
- `docs/rebaseline_artifacts/RB1-03.md`
- `docs/rebaseline_artifacts/RB1-04.md`
- `make init-db`
- `rg -n "check_m1_readiness|convert_to_hypertable|full_backfill_tushare|init_timescaledb|prepare_daily_bars_for_backfill|restore_daily_bars_compression|run_tushare_backfill|test_backfill|QUICKFIX_M1_TUSHARE_TOKEN" Makefile scripts README.md docs/README.md docs/EXECUTION_PLAN.md docs/PRD.md docs/ROADMAP.md`
