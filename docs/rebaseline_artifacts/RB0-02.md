# RB0-02

> 更新时间: 2026-04-07 00:00 CST
> 执行者: Codex
> 状态: review

## 任务目标

基于 `origin/main`、`docs/milestones/m0/M0_PR_BOUNDARY.md` 与 `#8` 合并结果，形成当前仓库的真实范围快照。

## 已完成

- 已确认 `origin/main` 存在 `a0a729f [M0] Phase 0/1 Baseline Freeze (#8)`。
- 已确认此前对 `#8` 的结论: 实际合并范围超出 `M0` 边界。
- 已建立 artifact 机制，后续可把后端、前端、文档的真实范围矩阵分别落盘。
- 已确认真实偏航的三条主线:
  - 运行时入口偏航: 后端额外挂载 `events/alert/strategy_social/optimization/report`，前端额外开放 `Alerts/StrategyMarket/Optimization/Reports`
  - 页面内部偏航: `Dashboard` / `Backtest` / `Attention` / `AppHeader` 存在 `P3` 入口和依赖
  - 文档资产偏航: `M1_*`、`PHASE3_*`、`*_v1_archive.md` 平铺在 `docs/` 根目录

## 关键结论

- `RB0-02` 是 `RB1-01` ~ `RB1-03` 的上游任务，但可边产出边驱动并行梳理。
- 本任务的最终结果将由三个范围矩阵和一个汇总文件共同支撑，而不是单个大段描述。
- 真实快照可先概括为:
  - `M0 keep`: Phase 0/1 主链路与其基础测试、验收文档
  - `remove`: 已暴露到运行时的 `P3-01/03/04/05/06`
  - `hold`: mixed file、阶段文档和后续阶段资产，需分阶段收口

## 剩余动作

1. 收集并汇总后端范围矩阵。
2. 收集并汇总前端范围矩阵。
3. 收集并汇总文档/脚本/测试范围矩阵。
4. 汇总为统一 keep/remove/hold 快照。

## 证据

- `docs/milestones/m0/M0_PR_BOUNDARY.md`
- `docs/REBASELINE_M0_PLAN.md`
- `app/api/routers/__init__.py`
- `app/main.py`
- `web/src/router/index.ts`
