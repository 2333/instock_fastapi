# RB2-04

> 更新时间: 2026-04-07 10:50 CST
> 执行者: Codex
> 状态: review

## 任务目标

清理运行时收口后的残余 wiring，包括残余 API 调用、状态轮询、store/composable 引用和构建残留。

## 已完成

- 已确认活跃页面上的 `useAnalytics` 残余引用已摘除:
  - `web/src/views/Dashboard.vue`
  - `web/src/views/Backtest.vue`
  - `web/src/views/Attention.vue`
  - `web/src/views/StockDetail.vue`
  - `web/src/views/Selection.vue`
- 已复扫前端剩余超范围引用，确认它们主要集中在待删除页面 / 组件:
  - `Alerts.vue`
  - `Optimization.vue`
  - `Reports.vue`
  - `ReportDetail.vue`
  - `ReportPreferences.vue`
  - `StrategyMarket.vue`
  - `NotificationBell.vue`
  - `StrategyCard.vue`
  - `StrategyDetailModal.vue`
  - `StrategyLeaderboard.vue`
  - `api/index.ts`
  - `api/alert.ts`
- 已执行前端轻量校验:
  - `cd web && npm run typecheck` 通过

## 关键结论

本任务不是第一刀，而是运行时入口收口后的扫尾层。

重点检查:

- `web/src/api/index.ts`
- `web/src/views/*` 中的延后接口调用
- `app/` 中仍被 import 的延后 service/schema
- 导航组件与路由守卫中的残余引用

本轮结论:

- 运行时主链路已经不再依赖 `alert/report/optimization/social/analytics`。
- 剩余超范围引用主要留存在已判定 `RB3-*` 将删除的页面、组件和 API 导出中。
- 因此 `RB2` 可以视为“运行时收口完成”，后续以 `RB3-02` / `RB3-04` 做物理删除和残余导入清扫更合适。

## 剩余动作

1. 在 `RB3-02` 删除前端超范围页面与组件。
2. 在 `RB3-04` 清理 `web/src/api/index.ts`、`web/src/api/alert.ts`、`useAnalytics.ts` 以及残余导入。
3. 在后端对应删除完成后，再做一次全仓引用复扫。

## 证据

- `docs/rebaseline_artifacts/RB1-04.md`
- `web/src/views/StockDetail.vue`
- `web/src/views/Selection.vue`
- `rg -n "useAnalytics|alertApi|strategySocialApi|optimizationApi|reportApi|NotificationBell|StrategyCard|StrategyDetailModal|StrategyLeaderboard" web/src`
- `cd web && npm run typecheck`
