# RB2-03

> 更新时间: 2026-04-07 10:35 CST
> 执行者: Codex
> 状态: review

## 任务目标

对混合型页面做片段级收口，移除不属于 `M0` 的卡片、标签页、入口和交互状态。

## 已完成

- 已对 `web/src/views/Dashboard.vue` 做片段级收口:
  - 移除热门策略卡片
  - 移除最新报告卡片
  - 移除参数优化卡片
  - 移除 `useAnalytics`、`strategySocialApi`、`reportApi`、`optimizationApi` 依赖与对应数据加载
- 已对 `web/src/views/Backtest.vue` 做片段级收口:
  - 保留 `my` 模板、保存配置、历史记录、对照回测、URL 状态
  - 移除 `public` / `optimization` 两个模板来源标签页
  - 移除 `StrategyCard`、`strategySocialApi`、`optimizationApi` 依赖与对应状态/方法
  - 移除 `useAnalytics` 埋点调用
- 已对 `web/src/views/Attention.vue` 做片段级收口:
  - 移除快速预警按钮与预警弹窗
  - 移除编辑面板中的 `alert_conditions` 录入
  - 移除 `alertApi` 与 `useAnalytics` 依赖
- 已执行前端轻量校验:
  - `cd web && npm run typecheck` 通过

## 关键结论

本轮收口结论:

- `Dashboard.vue` 已从 mixed view 回到 `M0` 工作台，只保留市场、关注、扫描、回测四块主链路信息。
- `Backtest.vue` 已收回到基础回测流，保留模板加载、参数配置、历史/对照、运行回测，不再承接公共策略和参数优化。
- `Attention.vue` 已收回到纯关注列表管理，不再承接预警创建与埋点逻辑。

收口原则已落实:

- 页面保留，但只保留 `M0` 需要的主链路能力。
- 不做整文件删除。
- 先删入口与状态，再删残余视图分支。

## 剩余动作

1. 在 `RB2-04` 继续核对运行时残余 wiring，重点看 `web/src/api/index.ts`、`StockDetail.vue`、`Selection.vue` 是否仍引用超范围 API / composable。
2. 在 `RB3-02` 删除已经失活的超范围页面与组件。
3. 在 `RB3-04` 统一清理前端残余导入、样式死代码与构建告警。

## 证据

- `docs/rebaseline_artifacts/RB1-02.md`
- `docs/rebaseline_artifacts/RB1-04.md`
- `web/src/views/Dashboard.vue`
- `web/src/views/Backtest.vue`
- `web/src/views/Attention.vue`
- `cd web && npm run typecheck`
