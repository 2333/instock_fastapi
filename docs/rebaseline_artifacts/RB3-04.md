# RB3-04

> 更新时间: 2026-04-07 12:25 CST
> 执行者: Codex
> 状态: review

## 任务目标

清理 `RB3-01`、`RB3-02`、`RB3-03` 后产生的残余依赖、导入和测试引用，确保仓库进入可验证状态。

## 当前已知残余

- 已删除 `tests/test_events_api.py`，因为对应的 `events_router` / `user_event_model` 已在 `RB3-01` 退场。
- 已清理 active `docs/EXECUTION_PLAN.md` 中对已删除 Timescale 过渡脚本的直接文件名引用。
- 已完成 active 代码、测试、脚本、Makefile 和主文档复扫。

## 验证要求

- 后端:
  - `rg` 复扫已删除后端符号在 `app tests scripts Makefile` 中无 active 命中。
  - `.venv/bin/python -c "import app.main; print('app.main import ok')"` 通过。
  - `.venv/bin/python -m pytest tests/test_router_endpoints.py -q` 通过，10 passed。
- 前端:
  - `cd web && npm run typecheck` 通过。
  - `rg` 复扫已删除前端符号在 `web/src` 中无命中。
- 文档/脚本:
  - `make init-db` 通过。
  - `rg` 复扫已删除脚本名在 `Makefile scripts README.md docs/README.md docs/EXECUTION_PLAN.md docs/PRD.md docs/ROADMAP.md` 中无坏入口引用。

## 剩余风险

- 里程碑归档文档 `docs/milestones/` 和治理 artifact 中仍会保留历史脚本名、任务名和证据命令；这些不是 active 入口。
- 更大范围的后端测试套件尚未完整运行，留给 `RB4-01` 做最终验收。
